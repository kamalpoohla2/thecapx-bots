# Procfile — for Railway.app / Render.com deployment
# If you want bots to run on a 24/7 server instead of GitHub Actions,
# connect this repo to Railway.app and it will use this file.

web: python orchestrator.py schedule
